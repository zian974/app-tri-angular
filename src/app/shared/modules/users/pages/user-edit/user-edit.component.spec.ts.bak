import { ComponentFixture, TestBed } from '@angular/core/testing';

import { TriEditComponent } from './tri-edit.component';

describe('TriEditComponent', () => {
  let component: TriEditComponent;
  let fixture: ComponentFixture<TriEditComponent>;

  beforeEach(async () => {
    await TestBed.configureTestingModule({
      declarations: [ TriEditComponent ]
    })
    .compileComponents();
  });

  beforeEach(() => {
    fixture = TestBed.createComponent(TriEditComponent);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
